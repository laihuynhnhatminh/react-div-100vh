{
  "name": "test-app",
  "description": "An example of client-side rendered app",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "parcel --dist-dir dist index.html",
    "build": "rimraf dist && parcel build --dist-dir dist index.html",
    "serve": "npx serve dist"
  },
  "dependencies": {
    "parcel": "^2.0.0",
    "react": "19",
    "react-dom": "19",
    "react-div-100vh": "*"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "rimraf": "6.0.1"
  }
}
